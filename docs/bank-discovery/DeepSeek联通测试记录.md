# DeepSeek Flash 联通测试记录

测试时间：2026-09-07（Asia/Shanghai）  
测试范围：仅验证网络、接口地址、鉴权和结构化请求；未发送供应商或银行业务数据  
基线提交：`f1f599bdc069bd62274ecb604921b3e8755fc792`

## 测试配置

- 接口格式：OpenAI 兼容 Chat Completions；
- Base URL：`https://api.deepseek.com`；
- 模型：`deepseek-v4-flash`；
- 输出要求：JSON object；
- 密钥位置：本地 `.env`，该文件被 Git 忽略；本文及 Git 仓库不记录密钥值。

## 测试结果

请求已成功到达 DeepSeek API，但服务器返回：

```text
HTTP 402 — Insufficient Balance
```

结论：

- 网络路径正常；
- DeepSeek 服务端已接收到请求；
- 当前阻断原因是账户余额不足，尚未获得一次完整模型输出；
- 补充账户余额或可用额度后，需使用相同的无业务数据测试再次验证；
- 在得到有效 JSON 输出并通过本地结构校验前，状态仍应记为“部分联通”，不能记为“模型调用验收通过”。

## 安全说明

- 测试未发送银行业务数据；
- 密钥不得提交 Git、写入测试报告或聊天转发；
- 因本次密钥已经通过聊天提供，建议完成测试后在平台侧轮换密钥；
- 后续使用银行真实或脱敏数据前，必须确认模型使用方式、数据边界、日志保留和运行环境。

## 下一次测试验收标准

1. API 返回 HTTP 2xx；
2. 返回内容是合法 JSON；
3. 本地 Pydantic 结构校验通过；
4. 日志中不出现密钥和业务原文；
5. 超时、额度不足和无效输出能显示明确错误并回退到安全流程。
