# 九江项目联调需求清单

版本：V1.0  
发起人：李芳痕（成员 C）  
用途：请成员 B、赵文雅及前端成员确认各自交付的接口、规则和展示边界，方便后续联调。

## 先统一一个事实

成员 C 已完成“模拟数据读取 → 特征整理 → 预测模型 → 多 Agent 流程 → 证据校验 → 候选建议 → 人工复核”的可运行原型。当前模型和数据均为**比赛模拟数据**，系统不会自动执行处置。

现阶段要联调的核心结果是一条“供应商某一周的风险研判结果”，至少包含：供应商编号、当前周、风险概率、是否预测升级、模型版本、风险趋势、关联证据、候选建议、处理状态和审计记录。

---

## 一、请成员 B（梁雨珊）确认和提供

### B-1 确认模拟数据 V1

请确认 2026-09-07 提供的以下 7 张 CSV 是否为后续开发使用的正式模拟数据 V1：

- `suppliers.csv`
- `contracts.csv`
- `projects.csv`
- `bank_systems.csv`
- `risk_events.csv`
- `rectify_records.csv`
- `weekly_snapshot.csv`

成员 C 已完成检查：200 家供应商、10,400 条周数据；无主键重复、无表间断链；事件数、风险分、整改状态和升级标签重新计算后与源文件一致。

请回复：`确认 V1 / 需替换`；如果替换，请给出文件版本号、更新时间和字段变更说明。

### B-2 确认数据字段的最终口径

请确认下列字段是否不会再改名或改变含义：

| 文件 | 当前关键字段 | 需要确认的内容 |
|---|---|---|
| `weekly_snapshot.csv` | `supplier_id, week, event_count, risk_score, has_rectify, business_exposure, upgrade_label` | 类型、是否允许为空、`(supplier_id, week)` 是否唯一 |
| `risk_events.csv` | `evidence_id, supplier_id, event_category, event_subtype, event_severity, event_week, source_type` | 事件类别枚举、严重程度范围、证据编号是否全局唯一 |
| `rectify_records.csv` | `rectify_id, supplier_id, start_week, end_week` | 整改结束周是否含当前周、是否有整改状态字段 |

其中 `upgrade_label=1` 当前按“从当前周之后 1 至 3 周内是否发生升级风险”理解；请确认此口径，避免训练与后端展示使用不同答案。

### B-3 提供后端接口草案或可调用 Mock 接口

前端和 Agent 联调至少需要以下能力；字段可调整，但请先固定请求和返回格式：

| 优先级 | 接口能力 | 最小请求 | 最小返回 |
|---|---|---|---|
| P0 | 供应商列表 | 页码、每页数量、风险等级筛选 | 供应商编号、名称、风险等级、最新风险分、最新周 |
| P0 | 供应商详情 | `supplier_id` | 基本信息、合同/项目/系统信息、周快照、风险事件、整改记录 |
| P0 | 发起一次风险研判 | `supplier_id`、`current_week` | 本清单开头所述的完整研判结果 |
| P1 | 处置中心列表 | 状态、优先级、分页 | 候选建议、证据状态、人工复核状态 |
| P1 | 提交人工复核结果 | 研判编号、审核人、结论、备注 | 更新后的处理状态和操作时间 |

建议接口路径仅作参考：`GET /suppliers`、`GET /suppliers/{supplier_id}`、`POST /risk-assessments`、`GET /disposals`、`POST /risk-assessments/{run_id}/review`。

### B-4 明确当前数据不能提前预测的边界

当前后段测试中存在“突发型”案例：风险发生前没有近期事件，因此模型没有足够的提前信号。若数据侧可补充，请优先提供以下按周记录的字段：

- SLA 达标率、SLA 违约次数；
- 项目延期天数或延期次数；
- 关键人员变动、人员缺口；
- 舆情早期信号；
- 交付质量、投诉、可用性波动。

请不要把案例类型、未来事件结果或人工设定答案直接放入特征字段，否则会造成“提前看到了答案”的假高分。

### B 的验收标准

- 以上 P0 接口可由前端或 Postman 成功调用；
- 请求错误时返回明确的 4xx 错误和说明；
- 同一 `supplier_id + current_week` 返回的基础数据与 CSV 一致；
- 接口文档标明字段类型、是否必填和示例；
- 数据或接口有变更时提供版本号和变更说明。

---

## 二、请赵文雅确认和提供

### Z-1 固化证据通过/不通过规则

目前代码里的最低规则是：证据必须存在、属于当前供应商、发生时间不得晚于当前研判周；不满足则阻断候选建议。

请确认正式规则：

- 哪些 `source_type` 可以作为有效证据；
- 是否要求证据严重程度达到门槛；
- 一条证据是否足够，还是需要多条/多类别证据；
- 整改完成的事件是否需要降权或不能作为证据；
- 证据不足时页面和后端应该显示什么原因。

交付建议：一张“条件 → PASS/FAIL → 页面提示文案”的规则表，并提供至少 5 条通过和 5 条不通过测试样例。

### Z-2 确认人工复核和处置门禁

系统当前只产生候选建议，最终状态始终是 `PENDING_HUMAN_REVIEW`，不自动发送告警、不自动整改、不自动停止服务。

请确定：

- 哪些风险等级必须人工确认；
- 谁能查看、谁能审核、谁能提交处置；
- 审核结论的枚举，例如“通过、驳回、补充证据”；
- 是否需要记录审核人、审核时间、备注和附件；
- 审核前后分别允许前端展示哪些信息。

### Z-3 明确审计日志与脱敏要求

现有流程每一步都会写入 Agent 名称、状态、时间和简要说明。请确定：

- 日志最少必须保留哪些字段；
- 哪些字段不能在前端直接展示；
- 证据内容、供应商名称、审核人信息是否需脱敏；
- 日志保存周期和导出权限；
- 比赛演示中允许展示哪些模拟内容。

### Z-4 提供可入库的制度和处置材料

RAG 知识库已经有读取、切分、索引和检索能力，但当前只装有示例文件。

请提供或确认可使用的：

- 银行外包风险管理制度节选；
- 风险等级与处置规则；
- 证据核验规则；
- 使用范围与来源说明。

每份文件请标明“可用于比赛演示 / 仅供内部参考 / 不可入库”。未经确认的文件不会放进知识库。

### Z-5 提供系统测试验收点

请给出至少以下四类测试场景及预期结果：

- 有完整证据的高风险案例；
- 证据编号不存在或属于其他供应商的案例；
- 引用了未来周证据的案例；
- 人工审核通过、驳回、要求补充的案例。

### 赵文雅的验收标准

- 证据 PASS/FAIL 规则可被写成确定条件，而不是只写“人工判断”；
- 人工复核状态、角色和可见范围明确；
- 至少一套可用于比赛的制度/规则材料有明确来源；
- 测试用例能覆盖正常、证据不足和权限受限三类情况。

---

## 三、请前端成员（马欣）确认和提供

### F-1 页面与最小展示范围

请确认以下四个页面作为第一轮联调范围：

| 页面 | P0 必须展示 | 当前可用数据 |
|---|---|---|
| 风险驾驶舱 | 高/中/低风险数量、近期研判概览、待人工复核数量 | 可由供应商列表和研判结果汇总 |
| 供应商列表 | 名称、编号、风险分、风险等级、最新状态 | `suppliers` + `weekly_snapshot` |
| 供应商详情 | 基础信息、风险趋势、历史事件、证据、整改记录 | 7 张 CSV / 后端详情接口 |
| 处置中心 | 候选建议、优先级、证据状态、人工复核状态 | Agent 研判结果 |

第一轮不要求图表一定完成；若做图表，优先“风险分随周变化”和“事件时间线”。

### F-2 需要按状态展示的文案

前端需要区分以下状态，不能统一显示成“已处置”：

| 状态 | 建议展示 |
|---|---|
| `PENDING_HUMAN_REVIEW` | 等待人工复核 |
| `EVIDENCE_INSUFFICIENT` | 证据不足，不能生成候选处置建议 |
| `PASS` | 证据校验通过 |
| `FAIL` | 证据校验不通过 |
| `MOCK_FALLBACK` | 示例预测，未使用训练模型 |
| `TRAINED_SIMULATION_MODEL` | 模拟数据训练模型，仅用于比赛演示 |

风险概率应展示为“风险升级参考概率”，并在旁标注“模拟数据结果，需人工复核”。当前不要把它直接翻译成正式风险等级，等待全组确定阈值。

### F-3 需要的接口联调格式

请按以下最小字段准备前端类型定义或 mock 数据：

```text
run_id
supplier_id
current_week
prediction.pred_upgrade_label
prediction.upgrade_probability
prediction.model_version
prediction.model_status
prediction_source
risk_trend.trend_type
risk_trend.trend_desc
evidence_result.status
evidence_result.evidence_items[]
candidate_actions[]
status
audit_trace[]
```

前端可先使用静态 Mock；成员 B 提供接口后，再替换为 API 调用。请保留加载中、空数据、接口错误和证据不足四种页面状态。

### F-4 前端验收标准

- 4 个页面能够互相跳转；
- 供应商列表可进入对应详情；
- 一条研判结果可完整展示预测、证据、候选建议和人工复核状态；
- 证据不足时不展示“可执行处置”；
- 页面清楚标示“模拟数据 / 等待人工复核”；
- 接口未完成时，Mock 数据可让演示不断链。

---

## 四、全组需要一起定下来的四件事

1. 是否正式在接口中保留 `upgrade_probability`，以及它是否允许前端展示；
2. `risk_level` 如何产生：谁生成、阈值是多少、是否有一票否决红线；
3. Agent 的输出字段版本号和错误返回格式；
4. 数据、模型、规则三者发生变化时，如何标记版本并同步修改。

## 五、建议回复格式

为提高对齐效率，请每位成员按下面格式回复即可：

```text
【姓名】
确认项：B-1 / Z-1 / F-1 ...
结论：确认 / 需调整
交付物：文件名、接口地址或规则表
预计提供时间：
需要成员 C 配合：
```

> 注：本清单中的字段和接口路径是联调草案，不代表最终银行生产规范；所有风险研判和处置仍须经过人工复核。
